.section .sys.version, "ax"

.global lib_mbedtls_2_26_0_version

lib_mbedtls_2_26_0_version:

#ifdef CONFIG_RELEASE_ENABLE

#else
.asciz __FILE__
.asciz "MBEDTLS_2_26_0-*modified*-@{DATE}-${COMMID}"
#endif
